<?xml version="1.0" encoding="utf-8"?>

<!--
<android.support.v4.view.ViewPager xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/pager"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />-->
<android.support.v4.view.ViewPager xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/pager"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F7F7F7">

    <!--
    This tab strip will display the currently visible page title, as well as the page
    titles for adjacent pages. we have other option pager title strip similar to pager tab strip
    -->
    <android.support.v4.view.PagerTabStrip android:id="@+id/pager_tab_strip"
        android:layout_width="match_parent"
        android:layout_height="150px"
        android:layout_gravity="top"
        android:background="#C9E8FF"
        android:textColor="#0099CC"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        android:textAppearance="@style/PagerTabStripText"/>

</android.support.v4.view.ViewPager>